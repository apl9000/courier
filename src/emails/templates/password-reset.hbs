{{!-- Header --}}
{{> header title="Reset Your Password"}}

{{!-- Reset Message --}}
<div class="email-section">
  <p class="email-body">Hi <strong>{{name}}</strong>,</p>
  <p class="email-body">
    We received a request to reset your password. Click the button below to create a new password:
  </p>
</div>

{{!-- CTA Button --}}
<div class="email-section email-text-center">
  <a href="{{resetUrl}}" class="email-button-primary">RESET PASSWORD</a>
</div>

{{!-- Reset Code --}}
{{#if resetCode}}
<div class="email-box-highlight">
  <p class="email-text-alt email-spacing-none">Or use this reset code:</p>
  <p class="email-spacing-sm">
    <span class="email-code-lg">{{resetCode}}</span>
  </p>
</div>
{{/if}}

{{!-- Expiry & Security Notice --}}
<div class="email-section">
  {{#if expiryHours}}
  <p class="email-text-alt">
    <em>This link will expire in {{expiryHours}} hours.</em>
  </p>
  {{/if}}
  <p class="email-text-alt">
    If you didn't request a password reset, please ignore this email or contact support if you have concerns.
  </p>
</div>
